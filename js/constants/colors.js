/* Nenhum valor hexadecimal deve ser escrito em JS.
   Sempre ler a cor real a partir da variável CSS correspondente. */
window.COLORS = Object.freeze({
  PRIMARY: "var(--primary-color)",
  SECONDARY: "var(--secondary-color)",
  ACCENT: "var(--accent)",
  GOLD: "var(--gold)",
  EMBER: "var(--ember)",
  BACKGROUND: "var(--background)",
  TEXT_PRIMARY: "var(--text-primary)",
  TEXT_SECONDARY: "var(--text-secondary)"
});
