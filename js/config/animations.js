window.CONFIG = window.CONFIG || {};

window.CONFIG.animations = {
  scrollRevealDuration: 900, // ms
  scrollRevealDistance: 40, // px
  kenBurnsDuration: 22000, // ms
  emberPulseDuration: 6000, // ms
  parallaxStrength: 0.15,
  navScrollThreshold: 60 // px scrolled antes de ativar nav.scrolled
};
